package mosi.bloom.earn.bud.services;

//import retrofit.SimpleXmlConverterFactory;

/**
 * Created by Harendra Kumar on 08-01-2016.
 */
public class MobikwikServiceGenerator {
    public static final String API_BASE_URL = "https://walletapi.mobikwik.com";

  /*  private static OkHttpClient
            httpClient = new OkHttpClient();

    Retrofit retrofit = new Retrofit.Builder()
            .baseUrl(API_BASE_URL)
            .addConverterFactory(SimpleXmlConverterFactory.create())
            .build();*/

  /*
    private static Retrofit.Builder builder =
            new Retrofit.Builder()
                    .baseUrl(API_BASE_URL)
                    .addConverterFactory(GsonConverterFactory.create());

*/

   /* public static <S> S createService(Class<S> serviceClass) {
        Retrofit retrofit = new Retrofit.Builder()
                .baseUrl(API_BASE_URL)
                .addConverterFactory(SimpleXmlConverterFactory.create())
                .build();
        return retrofit.create(serviceClass);
    }*/
}
